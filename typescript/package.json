{
  "name": "temporal-agents",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p .",
    "worker": "node --loader ts-node/esm src/worker.ts",
    "start-wf": "node --loader ts-node/esm src/client.starter.ts",
    "approve": "node --loader ts-node/esm src/client.signal.ts approve",
    "reject": "node --loader ts-node/esm src/client.signal.ts reject"
  },
  "dependencies": {
    "@temporalio/client": "^1.9.0",
    "@temporalio/worker": "^1.9.0",
    "@temporalio/workflow": "^1.9.0",
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
